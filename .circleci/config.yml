version: 2
jobs:
  build:
    docker:
      - image: circleci/rust:latest

    steps:
      - checkout
      - run: sudo apt-get update
      - run: sudo apt-get -y install libatk1.0-dev libpango1.0-dev libgtk-3-dev
      - run: make build
